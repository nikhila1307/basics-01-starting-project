let currentResult = 0;
function add(){
    const result = currentResult + userInput.textContent;
    outputResult(result, "")
}
addBtn.addEventListener('click', add)
addBtn.addEventListener('click', add)